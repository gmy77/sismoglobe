# Monitoraggio SismoGlobe post-fix v1.5.1

- 2026-08-17: ANOMALIA (nel processo di controllo, non necessariamente nel sito) — impossibile verificare i punti 1, 2 e 5 del controllo. Sia `https://sismo.gimmycloud.net/` sia `https://earthquake.usgs.gov/.../all_day.geojson` sono bloccati dalla policy di egress della rete della sessione automatica (errore `EGRESS_BLOCKED` / 403 dal proxy, confermato sia con curl sia con lo strumento di fetch web, con IP/DNS del proxy che rifiuta la CONNECT verso questi host). Non e' stato quindi possibile confermare lo status HTTP del sito, il conteggio eventi/24h del feed USGS, ne' la versione di main.js referenziata dall'index.html live. Controllo lato repository (punto 3) invece OK: `LIST_CAP = 300` presente in main.js (riga 596), usato correttamente in `renderList()`; nessun commit successivo al fix v1.5.1 (428e439) lo ha toccato o abbassato (`git log --oneline -10` pulito). Raccomandazione: verificare manualmente sito e feed, oppure abilitare questi due host nella policy di egress dell'ambiente usato per questo controllo automatico, altrimenti i prossimi run non potranno verificare la disponibilita' reale.
- 2026-08-18: ANOMALIA (persistente, nel processo di controllo, non nel sito) — stesso blocco di rete del 2026-08-17: `https://sismo.gimmycloud.net/` e `https://earthquake.usgs.gov/.../all_day.geojson` restituiscono ancora `EGRESS_BLOCKED`, confermato sia con curl sia con lo strumento WebFetch. Punti 1, 2 e 5 non verificabili per la seconda volta consecutiva. Controllo lato repository (punto 3) OK: `LIST_CAP = 300` in main.js (riga 597), usato correttamente in `renderList()`; nessun commit tra il fix v1.5.1 (428e439) e oggi ha toccato o abbassato la costante (`git log --oneline -10` pulito — solo commit feature successivi: EMSC live v1.6.0, cache-buster, badge fonti dati). Raccomandazione ribadita: abilitare `sismo.gimmycloud.net` ed `earthquake.usgs.gov` nella policy di egress di questo ambiente di controllo automatico, altrimenti questo monitoraggio continuera' a non poter verificare la disponibilita' reale di sito e feed.
- 2026-08-19: ANOMALIA (persistente per la terza volta consecutiva, nel processo di controllo, non confermata nel sito) — stesso blocco di rete di 08-17 e 08-18: sia `https://sismo.gimmycloud.net/` (curl e WebFetch) sia `https://earthquake.usgs.gov/.../all_day.geojson` (curl) restituiscono `EGRESS_BLOCKED`/403 dal proxy di egress della sessione (`recentRelayFailures` in `/__agentproxy/status` conferma `connect_rejected` su entrambi gli host). Punti 1, 2 e 5 non verificabili ancora una volta. Controllo lato repository (punto 3) OK: `LIST_CAP = 300` in main.js (riga 597), usato correttamente in `renderList()`; `git log --oneline -10` pulito, nessun commit ha toccato la costante da 428e439 (solo commit feature/UI successivi). `index.html` referenzia `main.js?v=1.6.0` (>= 1.5.1, OK). Raccomandazione ribadita per la terza volta: questo blocco di rete e' ormai persistente e impedisce da 3 giorni la verifica reale di sito e feed — servono le regole di egress abilitate per `sismo.gimmycloud.net` ed `earthquake.usgs.gov` nell'ambiente usato da questa routine schedulata.
- 2026-08-20: ANOMALIA (persistente per la quarta volta consecutiva, nel processo di controllo, non confermata nel sito) — stesso blocco di rete di 08-17/08-18/08-19: sia `https://sismo.gimmycloud.net/` sia `https://earthquake.usgs.gov/.../all_day.geojson` restituiscono ancora `403`/`connect_rejected` dal gateway di egress della sessione (confermato via curl e via `/__agentproxy/status`, che mostra `recentRelayFailures` alle 07:01:32-07:01:44 UTC di oggi per entrambi gli host, stesso pattern "gateway answered 403 to CONNECT"). Punti 1, 2 e 5 non verificabili per la quarta volta consecutiva. Controllo lato repository (punto 3) OK: `LIST_CAP = 300` in main.js (riga 597), usato correttamente in `renderList()`; `git log --oneline -10` pulito, nessun commit ha toccato la costante da 428e439 (solo commit feature/UI/monitoring successivi). `index.html` referenzia `main.js?v=1.6.0` (>= 1.5.1, OK). Raccomandazione ribadita per la quarta volta: il blocco di rete e' ormai cronico (4 giorni consecutivi) e impedisce qualunque verifica reale di sito e feed da questo ambiente — serve intervento umano per abilitare le regole di egress per `sismo.gimmycloud.net` ed `earthquake.usgs.gov` nell'ambiente usato da questa routine schedulata, altrimenti il monitoraggio automatico rimane cieco sui punti 1, 2 e 5 a tempo indeterminato.
